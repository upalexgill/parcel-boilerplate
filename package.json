{
  "name": "parcel-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf dist/* && parcel src/pages/*.html --open",
    "watch": "rm -rf dist/* && parcel dev watch src/pages/*.html --open",
    "build": "rm -rf dist/* && parcel build src/pages/*.html --no-content-hash"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.43.4"
  }
}
